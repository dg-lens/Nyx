{
  "name": "@nyx/sync",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "main": "dist/cli/sync.js",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "start": "node dist/cli/sync.js",
    "dev": "tsx src/cli/sync.ts",
    "once": "node dist/cli/sync.js --once",
    "dev:once": "tsx src/cli/sync.ts --once"
  },
  "dependencies": {
    "@nyx/dispatcher": "workspace:*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
