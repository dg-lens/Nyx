# Nyx pipeline — retired tier doc

Knowledge for this subsystem lives in the **Arachne memory graph**
(`~/Nyx/Data/memory`, scope `stack.nyx.pipeline`), read via the `memory_*` MCP tools.
Enter at `moc-nyx-pipeline` (+ `moc-nyx-pipeline-redux`, `moc-nyx-pipeline-shipping`).

Historical tier-3 content of this file is recoverable from git history.
